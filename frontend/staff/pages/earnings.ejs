<style>
        body { font-family: 'Plus Jakarta Sans', sans-serif; background-color: #f8fafc; }
        
        .glass-panel {
            background: white;
            border: 1px solid #e2e8f0;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.02), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            border-radius: 0.5rem;
        }

        .animate-slide-up { animation: slideUp 0.4s ease-out forwards; opacity: 0; }
        @keyframes slideUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }

        /* Print Logic: Hide everything except the Payslip View */
        @media print {
            body * { visibility: hidden; }
            #payslipView, #payslipView * { visibility: visible; }
            #payslipView { 
                position: absolute; 
                left: 0; 
                top: 0; 
                width: 100%; 
                margin: 0; 
                padding: 0; 
                background: white; 
            }
            .no-print { display: none !important; }
            .print-border { border: none !important; box-shadow: none !important; }
        }
    </style>
<div class="text-slate-800 h-screen flex flex-col overflow-hidden">

    <div class="bg-slate-900 text-white p-4 flex justify-between items-center shadow-lg z-30 no-print">
        <div class="flex items-center gap-3">
            <div class="w-8 h-8 rounded-lg bg-teal-500 flex items-center justify-center font-bold">
                <i class="fas fa-wallet"></i>
            </div>
            <h1 class="text-lg font-bold tracking-tight">MY FINANCES</h1>
        </div>
        <button onclick="window.location.reload()" class="text-xs font-bold text-slate-400 hover:text-white transition">
            <i class="fas fa-sync-alt mr-1"></i> REFRESH
        </button>
    </div>

    <div class="flex-1 overflow-y-auto bg-slate-50 relative p-4 md:p-8" id="mainContainer">
        
        <div id="loading" class="flex flex-col items-center justify-center h-full text-slate-400">
            <i class="fas fa-circle-notch fa-spin text-3xl mb-3 text-teal-500"></i>
            <p class="text-sm font-semibold">Securely fetching data...</p>
        </div>

        <div id="dashboardView" class="hidden max-w-full mx-auto space-y-6 animate-slide-up">
            
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                <div class="glass-panel p-6 border-l-4 border-indigo-500">
                    <div class="flex justify-between items-start mb-4">
                        <div class="w-10 h-10 rounded-full bg-indigo-50 flex items-center justify-center text-indigo-600">
                            <i class="fas fa-coins text-lg"></i>
                        </div>
                        <span class="text-[10px] font-black bg-indigo-100 text-indigo-700 px-2 py-1 rounded uppercase">Current</span>
                    </div>
                    <p class="text-xs font-bold text-slate-400 uppercase tracking-widest">Basic Salary</p>
                    <h2 class="text-2xl font-black text-slate-800 mt-1" id="dispBasic">KES 0.00</h2>
                    <p class="text-xs text-slate-500 mt-2" id="dispPosition">Loading...</p>
                </div>

                <div class="glass-panel p-6 border-l-4 border-teal-500">
                    <div class="flex justify-between items-start mb-4">
                        <div class="w-10 h-10 rounded-full bg-teal-50 flex items-center justify-center text-teal-600">
                            <i class="fas fa-chart-line text-lg"></i>
                        </div>
                    </div>
                    <p class="text-xs font-bold text-slate-400 uppercase tracking-widest">Allowances & Comm.</p>
                    <h2 class="text-2xl font-black text-slate-800 mt-1" id="dispAllowances">KES 0.00</h2>
                    <p class="text-xs text-slate-500 mt-2">Bonuses & Commissions</p>
                </div>

                <div class="glass-panel p-6 border-l-4 border-emerald-500 bg-emerald-50/50">
                    <div class="flex justify-between items-start mb-4">
                        <div class="w-10 h-10 rounded-full bg-emerald-100 flex items-center justify-center text-emerald-600">
                            <i class="fas fa-check-circle text-lg"></i>
                        </div>
                    </div>
                    <p class="text-xs font-bold text-emerald-600 uppercase tracking-widest">Net Take Home</p>
                    <h2 class="text-2xl font-black text-emerald-800 mt-1" id="dispNet">KES 0.00</h2>
                    <p class="text-xs text-emerald-600 mt-2">Estimated Monthly</p>
                </div>
            </div>

            <div class="glass-panel overflow-hidden">
                <div class="px-6 py-4 border-b border-slate-100 bg-white flex justify-between items-center">
                    <h3 class="font-bold text-slate-700">Payslip History</h3>
                    <span class="text-xs font-bold text-slate-400 uppercase" id="payslipCount">0 Records</span>
                </div>
                <div class="overflow-x-auto">
                    <table class="w-full text-left">
                        <thead class="bg-slate-50 border-b text-slate-500">
                            <tr>
                                <th class="px-6 py-3 text-xs font-bold uppercase tracking-wider">Period</th>
                                <th class="px-6 py-3 text-xs font-bold uppercase tracking-wider">Date</th>
                                <th class="px-6 py-3 text-xs font-bold uppercase tracking-wider">Net Pay</th>
                                <th class="px-6 py-3 text-xs font-bold uppercase tracking-wider text-right">Action</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-slate-100 bg-white" id="payslipTableBody"></tbody>
                    </table>
                </div>
            </div>
        </div>

        <div id="payslipView" class="hidden max-w-3xl mx-auto animate-slide-up">
            <div class="mb-6 flex justify-between items-center no-print">
                <button onclick="showDashboard()" class="flex items-center gap-2 text-sm font-bold text-slate-500 hover:text-slate-800 transition">
                    <i class="fas fa-arrow-left"></i> Back to Dashboard
                </button>
                <button onclick="window.print()" class="bg-slate-800 text-white px-4 py-2 rounded-lg text-sm font-bold hover:bg-slate-900 transition flex items-center gap-2">
                    <i class="fas fa-print"></i> Print Payslip
                </button>
            </div>

            <div class="glass-panel p-8 md:p-12 print-border bg-white" id="payslipPaper">
                <div class="border-b-2 border-slate-800 pb-6 mb-8 flex justify-between items-end">
                    <div>
                        <h1 class="text-3xl font-black text-slate-900 tracking-tight">PAYSLIP</h1>
                        <p class="text-sm text-slate-500 font-bold uppercase tracking-widest mt-1">Dr.Net Technology Labs</p>
                    </div>
                    <div class="text-right">
                        <p class="text-xs text-slate-400 uppercase font-bold">Pay Period</p>
                        <p class="text-xl font-bold text-slate-800" id="psPeriod">--</p>
                    </div>
                </div>

                <div class="grid grid-cols-2 gap-8 mb-8">
                    <div>
                        <p class="text-[10px] font-bold text-slate-400 uppercase tracking-widest mb-1">Employee</p>
                        <p class="font-bold text-slate-900 text-lg" id="psName">--</p>
                        <p class="text-sm text-slate-600" id="psPosition">--</p>
                        <p class="text-xs text-slate-400 mt-1">ID: <span id="psId">--</span></p>
                    </div>
                    <div class="text-right">
                        <p class="text-[10px] font-bold text-slate-400 uppercase tracking-widest mb-1">Payment</p>
                        <p class="font-bold text-slate-900" id="psDate">--</p>
                    </div>
                </div>

                <table class="w-full mb-8 border-collapse">
                    <thead class="bg-slate-50 border-y-2 border-slate-100">
                        <tr>
                            <th class="py-3 text-left text-xs font-bold text-slate-500 uppercase tracking-wider">Description</th>
                            <th class="py-3 text-right text-xs font-bold text-slate-500 uppercase tracking-wider">Amount (KES)</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-slate-100 text-sm">
                        <tr>
                            <td class="py-3 font-medium text-slate-700">Basic Salary</td>
                            <td class="py-3 text-right font-bold text-slate-800" id="psBasic">0.00</td>
                        </tr>
                        <tr>
                            <td class="py-3 text-slate-600">
                                Allowances & Bonuses <br>
                                <span class="text-[10px] text-slate-400 italic" id="psAllowDesc">--</span>
                            </td>
                            <td class="py-3 text-right font-medium text-emerald-600" id="psAllow">0.00</td>
                        </tr>
                        <tr class="bg-slate-50/50">
                            <td class="py-3 font-bold text-slate-800 pl-2">GROSS PAY</td>
                            <td class="py-3 text-right font-bold text-slate-900" id="psGross">0.00</td>
                        </tr>
                        
                        <tr>
                            <td class="py-3 text-slate-600">
                                Deductions <br>
                                <span class="text-[10px] text-slate-400 italic" id="psDedDesc">--</span>
                            </td>
                            <td class="py-3 text-right font-medium text-rose-600" id="psDeduct">- 0.00</td>
                        </tr>
                    </tbody>
                    <tfoot class="border-t-2 border-slate-800">
                        <tr>
                            <td class="py-4 text-xl font-black text-slate-800">NET PAY</td>
                            <td class="py-4 text-right text-xl font-black text-slate-900" id="psNet">0.00</td>
                        </tr>
                    </tfoot>
                </table>

                <div class="mt-12 pt-6 border-t border-slate-200 flex justify-between items-end">
                    <div>
                        <p class="text-[10px] text-slate-400 uppercase font-bold tracking-widest">System Generated</p>
                        <p class="text-[9px] text-slate-300 mt-1">This document is valid without a signature.</p>
                    </div>
                    <div class="text-right">
                        <img src="/static/images/logo_image.jpg" onerror="this.style.display='none'" class="h-8 opacity-50 grayscale" alt="Company Logo">
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>
    <div id="toastContainer" class="fixed top-5 right-5 z-50 space-y-3 no-print"></div>

    <script>
        // --- Utilities ---
        const formatMoney = (amount) => new Intl.NumberFormat('en-KE', { style: 'currency', currency: 'KES' }).format(amount || 0);
        const formatDate = (dateStr) => dateStr ? new Date(dateStr).toLocaleDateString('en-GB', { day: 'numeric', month: 'short', year: 'numeric' }) : '-';

        // --- Core Logic ---
        
        window.onload = () => loadDashboardData();

        async function loadDashboardData() {
            try {
                const res = await fetch('/api/finance/my-finance');
                const data = await res.json();
                
                if (data.success) {
                    renderCards(data.salary);
                    renderHistory(data.payslips);
                    document.getElementById('loading').classList.add('hidden');
                    document.getElementById('dashboardView').classList.remove('hidden');
                } else {
                    showToast("Failed to load data", "error");
                }
            } catch (err) {
                console.error(err);
                showToast("Connection error", "error");
            }
        }

        function renderCards(salary) {
            if (!salary) return;
            const basic = parseFloat(salary.basic_salary) || 0;
            const extras = (parseFloat(salary.commision) || 0) + (parseFloat(salary.bonuses) || 0);
            const net = parseFloat(salary.net_salary) || 0;

            document.getElementById('dispBasic').innerText = formatMoney(basic);
            document.getElementById('dispAllowances').innerText = formatMoney(extras);
            document.getElementById('dispNet').innerText = formatMoney(net);
            document.getElementById('dispPosition').innerText = `${salary.position || 'Staff'} • ${salary.department || ''}`;
        }

        function renderHistory(list) {
            const tbody = document.getElementById('payslipTableBody');
            document.getElementById('payslipCount').innerText = `${list.length} Records`;

            if (list.length === 0) {
                tbody.innerHTML = `<tr><td colspan="4" class="p-8 text-center text-slate-400 italic">No payslips found.</td></tr>`;
                return;
            }

            tbody.innerHTML = list.map(p => `
                <tr class="hover:bg-slate-50 transition border-b border-slate-50 last:border-none">
                    <td class="px-6 py-4 text-sm font-bold text-slate-700">${p.pay_period}</td>
                    <td class="px-6 py-4 text-xs text-slate-500">${formatDate(p.payment_date)}</td>
                    <td class="px-6 py-4 text-sm font-black text-emerald-600">${formatMoney(p.net_pay)}</td>
                    <td class="px-6 py-4 text-right">
                        <button onclick="viewPayslip(${p.id})" class="text-xs bg-slate-800 text-white px-3 py-1.5 rounded-md font-bold hover:bg-slate-900 transition shadow-sm hover:shadow-md">
                            VIEW
                        </button>
                    </td>
                </tr>
            `).join('');
        }

        // --- View Switching Logic ---

        async function viewPayslip(id) {
            // Show loading overlay if needed, or simple toast
            showToast("Loading document...", "success");

            try {
                const res = await fetch(`/api/finance/payslip/${id}`);
                const data = await res.json();

                if (data.success) {
                    populatePayslip(data.payslip);
                    // Hide Dashboard, Show Payslip
                    document.getElementById('dashboardView').classList.add('hidden');
                    document.getElementById('payslipView').classList.remove('hidden');
                    // Scroll to top
                    document.getElementById('mainContainer').scrollTop = 0;
                }
            } catch (err) {
                showToast("Error loading document", "error");
            }
        }

        function showDashboard() {
            document.getElementById('payslipView').classList.add('hidden');
            document.getElementById('dashboardView').classList.remove('hidden');
        }

        function populatePayslip(p) {
                // Parse JSON arrays safely
                let allowancesArray = [];
                let deductionsArray = [];
                
                try {
                    allowancesArray = typeof p.allowances === 'string' ? JSON.parse(p.allowances) : (Array.isArray(p.allowances) ? p.allowances : []);
                } catch (e) {
                    console.warn('Failed to parse allowances:', e);
                    allowancesArray = [];
                }
                
                try {
                    deductionsArray = typeof p.deductions === 'string' ? JSON.parse(p.deductions) : (Array.isArray(p.deductions) ? p.deductions : []);
                } catch (e) {
                    console.warn('Failed to parse deductions:', e);
                    deductionsArray = [];
                }
                
                // Calculate display totals from arrays
                const totalAllowances = allowancesArray.reduce((sum, val) => sum + parseFloat(val || 0), 0);
                const totalDeductions = deductionsArray.reduce((sum, val) => sum + parseFloat(val || 0), 0);
                
                // Use saved values directly
                const gross = parseFloat(p.gross_pay) || 0;
                const net = parseFloat(p.net_pay) || 0;
                
                // Basic = Gross - Allowances (for display breakdown)
                const basicSalary = gross - totalAllowances;
                
                // Set employee info
                document.getElementById('psPeriod').innerText = p.pay_period || 'N/A';
                document.getElementById('psName').innerText = `${p.first_name || ''} ${p.second_name || ''}`.trim();
                document.getElementById('psPosition').innerText = p.position || 'Staff';
                document.getElementById('psId').innerText = p.employee_id || 'N/A';
                document.getElementById('psDate').innerText = formatDate(p.payment_date);
                
                // Display financial breakdown
                document.getElementById('psBasic').innerText = formatMoney(basicSalary);
                document.getElementById('psAllow').innerText = formatMoney(totalAllowances);
                document.getElementById('psAllowDesc').innerText = p.allowance_description || "Standard Allowances";
                
                document.getElementById('psGross').innerText = formatMoney(gross);
                
                document.getElementById('psDeduct').innerText = "- " + formatMoney(totalDeductions);
                document.getElementById('psDedDesc').innerText = p.deduction_description || "Tax & Statutory";
                
                document.getElementById('psNet').innerText = formatMoney(net);
            }

        // --- Toast Notification ---
        function showToast(msg, type) {
            const container = document.getElementById('toastContainer');
            const el = document.createElement('div');
            const bg = type === 'error' ? 'bg-rose-600' : 'bg-emerald-600';
            el.className = `${bg} text-white p-4 rounded-xl shadow-xl min-w-[200px] animate-slide-up`;
            el.innerHTML = `<p class="text-xs font-bold uppercase opacity-75">${type}</p><p class="text-sm font-bold">${msg}</p>`;
            container.appendChild(el);
            setTimeout(() => el.remove(), 3000);
        }
    </script>